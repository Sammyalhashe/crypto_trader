name: C/C++ CI

on:
  push:
      branches: [ master ]
  pull_request:
      branches: [ master ]

jobs:
  build-and-test:
      runs-on: ubuntu-latest
      steps:
      - name: install cppunit
      run: sudo apt install -y python3.8
      - name: build
      run: make build
